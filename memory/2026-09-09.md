
## Fix espacios en blanco landing (12:58 UTC)
- **Problema 1 (desktop/tablet/landscape):** tarjeta "Calibración del modelo" (edge-mock) se estiraba a la altura del mockup del teléfono (448px) por `align-items: stretch` en `.preview-grid`, pero su contenido solo ocupa ~300px; `margin-top: auto` en `.edge-foot` empujaba el pie al fondo → hueco negro de ~152px (30% de la tarjeta).
- **Fix:** en templates/index.html (inline style): `.edge-mock` ahora tiene `justify-content: space-between` y `.edge-foot` `margin-top: 1.2rem` (antes `auto`). Resultado: gap barras→pie pasó de 152px a 58-66px en desktop y 37px en móvil.
- **Problema 2 (móviles altos):** `.hero` con `min-height: 100vh` + `justify-content: center` creaba ~113px de aire muerto entre stats-bar y la sección 2 en pantallas altas (932px).
- **Fix:** media query `@media (max-width: 600px) { .hero { min-height: auto; } }` en el style inline de index.html. Gap stats→siguiente sección ahora 48px constante.
- Backup: backups_predicta_fix/index.html.pre_whitespace_fix_20260909
- Reinicio predicta hecho (template cache DEBUG=False). Verificado curl HTTP 200, sin errores JS en consola.
